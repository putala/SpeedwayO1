<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.putala.speedwayo1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeedwayO1">
        <activity android:name=".activites.MainActivity">

        </activity>
        <activity android:name=".BrudnopisActivity" />
        <activity android:name=".activites.SettingsActivity" />
        <activity android:name=".activites.RankingActivity" />

        <activity
            android:name=".smieci.MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.SpeedwayO1.NoActionBar" />
        <activity android:name=".activites.ContestActivity" />

        <activity android:name=".registration.RegistrationActivity" />

        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>